import styled from "styled-components";

export const SliderContainer = styled.div`
    grid-area: slider;
    margin-top: 2rem;
    padding: 2rem;
`;

export const SliderObject = styled.input`
    width: 100%;
    cursor: pointer;
`;